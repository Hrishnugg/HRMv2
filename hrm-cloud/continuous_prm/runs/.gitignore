# Run artifacts: keep text results (csv/json/md/log), exclude heavy/reproducible binaries.
*.pt
*.npz
*.pth
*.ckpt
figures/
*.png
*.jpg
*.gif
# Intermediate per-suite eval shards (redundant with the merged *_eval_raw.csv).
_shards/
